{
  "name": "sw-beauty-salon-e2e",
  "version": "0.0.0",
  "private": true,
  "description": "Playwright e2e harness for SW Beauty Salon — not shipped, dev only.",
  "scripts": {
    "test:e2e": "playwright test --reporter=list"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
